import type { Metadata } from "next"; import { PublicShell } from "@/components/public-shell"; import { PromptCoverageChecker } from "@/components/prompt-coverage-checker"; import { pageMetadata } from "@/lib/seo";
export const metadata: Metadata = pageMetadata({ title: "Free AI Prompt Coverage Checker", description: "Run one live buyer question and inspect whether your brand appears and which web sources the provider returns.", path: "/prompt-check" });
export default function PromptCheckPage() { return <PublicShell><section className="page-hero page-hero--blue"><div className="shell narrow-heading"><span className="eyebrow">One prompt · one dated answer</span><h1>Check the exact buyer question that matters.</h1><p>See whether your brand appears, read the returned answer, and inspect the provider-returned citations. No signup required.</p></div></section><section className="section section--paper"><div className="shell narrow"><PromptCoverageChecker /></div></section></PublicShell>; }
