import { NextResponse } from "next/server"; import { getViewer } from "@/lib/auth"; import { getPrimaryWorkspaceRole, loadWorkspaceContext } from "@/lib/data"; import { exchangeGoogleCode, saveGoogleSheetsConnection } from "@/lib/google-sheets-connector"; import { verifyOAuthState } from "@/lib/oauth-state";
export async function GET(request: Request) { const viewer = await getViewer(); const settings = new URL("/app/settings?integration=google-sheets", process.env.NEXT_PUBLIC_SITE_URL || request.url); if (!viewer || viewer.mode === "demo") return NextResponse.redirect(new URL("/login?next=/app/settings", settings)); const [context, role] = await Promise.all([loadWorkspaceContext(viewer), getPrimaryWorkspaceRole(viewer)]); const url = new URL(request.url); const code = url.searchParams.get("code") || ""; const state = url.searchParams.get("state") || ""; if (!context || (role !== "owner" && role !== "admin") || !code || !await verifyOAuthState(state, "google_sheets", context.organizationId, viewer.id, process.env.GOOGLE_OAUTH_STATE_SECRET || "")) { settings.searchParams.set("status", "invalid"); return NextResponse.redirect(settings); } try { const redirectUri = `${process.env.NEXT_PUBLIC_SITE_URL}/api/integrations/google-sheets/callback`; await saveGoogleSheetsConnection(context.organizationId, context.projectId, viewer.id, await exchangeGoogleCode(code, redirectUri)); settings.searchParams.set("status", "connected"); } catch { settings.searchParams.set("status", "failed"); } return NextResponse.redirect(settings); }
