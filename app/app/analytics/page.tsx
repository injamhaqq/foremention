import { requireViewer } from "@/lib/auth";

const trend = [18, 21, 20, 24, 27, 31, 34, 38];
const referrals = [["ChatGPT", 42, 4], ["Perplexity", 31, 3], ["Gemini", 18, 1], ["Claude", 9, 1]] as const;

export default async function AnalyticsPage() { const viewer = await requireViewer("/app/analytics"); if (viewer.mode !== "demo") return <main className="workspace"><div className="workspace-heading"><div><span className="eyebrow">Observed performance</span><h1>Recommendation analytics</h1><p>Analytics appear only after Foremention has dated, reviewed observations. The product never substitutes demo metrics for customer data.</p></div></div><section className="panel empty-state empty-state--border"><h2>Analytics will begin after your first reviewed collection run.</h2><p>Recommendation share, first-mention share, citations, and referrals stay empty until they are backed by your workspace records.</p></section></main>; return <main className="workspace"><div className="workspace-heading"><div><span className="eyebrow">Observed performance</span><h1>Recommendation analytics</h1><p>Sample metrics distinguish measured channel changes from business contribution. No chart claims that a source action caused revenue.</p></div><select aria-label="Analytics window" defaultValue="Last 8 weeks"><option>Last 8 weeks</option><option>Last 30 days</option><option>Quarter to date</option></select></div><div className="metric-grid"><article><span>Recommendation share</span><strong>38%</strong><small>+7 points vs baseline</small></article><article><span>First-mention share</span><strong>12%</strong><small>+3 points vs baseline</small></article><article><span>Supporting citations</span><strong>31</strong><small>9 recurring sources</small></article><article><span>Verified AI referrals</span><strong>100</strong><small>9 reported conversions</small></article></div><div className="analytics-grid"><section className="panel panel--wide"><span className="eyebrow">Recommendation share</span><h2>Eight-week observation trend</h2><div className="trend-chart" role="img" aria-label="Recommendation share rises from 18 to 38 percent across eight weeks">{trend.map((value,index) => <div key={index}><i style={{height: `${value * 2}%`}} /><span>W{index+1}</span><small>{value}%</small></div>)}</div><p className="table-caption">Directional sample. Real customer reporting retains run IDs, prompt sets, providers, dates, and confidence notes.</p></section><section className="panel"><span className="eyebrow">AI referrals</span><h2>Verified sessions by source</h2><div className="referral-list">{referrals.map(([source,sessions,conversions]) => <div key={source}><div><strong>{source}</strong><span>{sessions} sessions</span></div><b>{conversions} conversions</b></div>)}</div></section><section className="panel"><span className="eyebrow">Attribution confidence</span><h2>Three evidence levels</h2><div className="confidence-list"><div><strong>Verified referral</strong><span>100 sessions</span></div><div><strong>Self-reported AI discovery</strong><span>14 leads</span></div><div><strong>Source-action influenced journey</strong><span>3 opportunities</span></div></div></section></div></main>; }
