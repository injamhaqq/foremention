import { WorkspaceListSkeleton } from "@/components/workspace-list-skeleton";

export default function PassportLoading() {
  return <WorkspaceListSkeleton label="Vendor Passport record" rows={3} />;
}
