# Architecture Radar Refinement Release Note

This branch changes documentation only. It does not alter runtime code, schema, RLS, authentication, providers, Cloudflare bindings, Inngest jobs, or customer evidence.

Changes:

- Firecrawl: REJECT → BENCHMARK ONLY.
- Activepieces: REJECT → FUTURE / STRATEGIC.
- Novu: REJECT → FUTURE.
- Adds a production canonicalization checklist for the current main release.
- Adds the ordered P2 engineering plan after production acceptance.

No production dependency is added by this branch.
