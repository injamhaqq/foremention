import Image from "next/image"; import Link from "next/link"; import { verifiedSocialProof } from "@/lib/social-proof";
export function VerifiedSocialProof() { const proof = verifiedSocialProof(); if (!proof.ready) return null; return <section className="section section--paper" aria-label="Verified customer evidence"><div className="shell"><div className="platform-heading"><span className="goat-kicker">Customer evidence</span><h2>What verified customers report.</h2></div><div className="customer-logo-bar">{proof.logos.map((logo) => <Image key={logo.name} src={logo.imagePath} alt={logo.name} width={160} height={48} />)}</div><div className="platform-steps">{proof.quotes.map((quote) => <figure key={`${quote.company}-${quote.person}`}><blockquote>{quote.quote}</blockquote><figcaption>{quote.person} · {quote.role}, {quote.company}</figcaption></figure>)}</div>{proof.studies.map((study) => <article key={study.href} className="panel"><span className="eyebrow">Verified case study</span><h3>{study.title}</h3><p>{study.summary}</p><Link href={study.href}>Read the evidence →</Link></article>)}</div></section>; }
