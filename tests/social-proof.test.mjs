import assert from "node:assert/strict"; import fs from "node:fs"; import path from "node:path"; import test from "node:test"; const read = (file) => fs.readFileSync(path.join(process.cwd(), file), "utf8");
test("social proof placeholders cannot render without verification and evidence", () => { const data = read("lib/social-proof.ts"); const component = read("components/verified-social-proof.tsx"); assert.match(data, /PLACEHOLDER/); assert.match(data, /verified: false/); assert.match(data, /evidenceUrl\.startsWith\("https:\/\/"\)/); assert.match(component, /if \(!proof\.ready\) return null/); });
