import assert from "node:assert/strict"; import fs from "node:fs"; import path from "node:path"; import test from "node:test";
const read = (file) => fs.readFileSync(path.join(process.cwd(), file), "utf8");
test("Notion exports only a tenant-scoped recorded weekly Source Map digest", () => { const source = read("lib/notion-connector.ts"); assert.match(source, /organization_id=eq\.\$\{organizationId\}/); assert.match(source, /provider=eq\.notion/); assert.match(source, /provider-returned citations/); assert.doesNotMatch(source, /guarantee|ranking improvement/i); });
test("Notion OAuth is state-bound and credentials stay server-side", () => { const callback = read("app/api/integrations/notion/callback/route.ts"); const migration = read("supabase/migrations/20260802000800_hubspot_activity_connector.sql"); assert.match(callback, /verifyOAuthState/); assert.match(migration, /integration_credentials/); assert.doesNotMatch(read("components/notion-settings.tsx"), /access_token|refresh_token/); });
