import assert from "node:assert/strict"; import fs from "node:fs"; import path from "node:path"; import test from "node:test"; const read = (file) => fs.readFileSync(path.join(process.cwd(), file), "utf8");
test("public visibility score uses five live provider answers and never fabricates a fallback", () => { const worker = read("worker/index.ts"); assert.match(worker, /answers\.length !== 5/); assert.match(worker, /No result was invented/); assert.match(worker, /appearedIn/); assert.match(worker, /methodology/); assert.doesNotMatch(worker, /Math\.random/); });
test("public visibility score is cost-capped, hashed-rate-limited, cached, and shareable", () => { const worker = read("worker/index.ts"); const schema = read("db/schema.ts"); const ui = read("components/visibility-score-form.tsx"); assert.match(worker, /PUBLIC_TOOL_MAX_REQUEST_COST_USD/); assert.match(worker, /sha256/); assert.match(schema, /public_visibility_scores/); assert.match(ui, /Copy share link/); assert.match(ui, /Three checks per day/); });
