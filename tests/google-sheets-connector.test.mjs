import assert from "node:assert/strict"; import fs from "node:fs"; import path from "node:path"; import test from "node:test"; const read = (file) => fs.readFileSync(path.join(process.cwd(), file), "utf8");
test("Google Sheets exports only allowlisted organization-scoped workspace tables", () => { const source = read("lib/google-sheets-connector.ts"); assert.match(source, /SHEETS_DATASETS/); assert.match(source, /organization_id=eq\.\$\{organizationId\}/); assert.match(source, /limit=5000/); assert.doesNotMatch(source, /SELECT \*|from\(body/i); });
test("Google Sheets OAuth uses offline access and server-side encrypted credentials", () => { const connect = read("app/api/integrations/google-sheets/connect/route.ts"); const source = read("lib/google-sheets-connector.ts"); assert.match(connect, /access_type: "offline"/); assert.match(source, /encryptIntegrationCredential/); assert.doesNotMatch(read("components/google-sheets-settings.tsx"), /access_token|refresh_token/); });
