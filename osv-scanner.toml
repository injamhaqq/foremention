# OSV exceptions must be narrow, documented, and time-bounded.
# Production dependencies remain separately blocked by `pnpm audit --prod` in CI.

[[IgnoredVulns]]
id = "GHSA-f8m6-h2c7-8h9x"
ignoreUntil = 2026-09-14
reason = "Transitive image-size 2.0.2 advisory is confined to the current vinext build/dev dependency chain; no upstream fixed version is available in that toolchain yet. Production dependency audit remains blocking. Re-evaluate before this exception expires."
