name: Restore Drill Signup (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "One-shot signup workflow retired after successful confirmation."
